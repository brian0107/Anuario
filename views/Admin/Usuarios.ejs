<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css"/>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Anuario: Admin Usuarios</title>
  </head>
  <body>
    <div style="padding: 0px 75px 0px 80px;" class="container-sm">
        <%- include('Header.ejs') %>
    
        <a style="margin-top: 20px;" class="btn btn-success" href="RegistrarUsuario">Nuevo Usuario</a>

        <h2 class="text-center">Usuarios</h2> <!-- Subtitulo para informar que se muestran las propiedades en la tabla de abajo -->

        <table class="table">
      
          <thead>
            <tr>
              <th scope="col" class="text-center">Correo</th>
              <th scope="col" class="text-center">Contraseña</th>
              <th scope="col" class="text-center">Admin</th>
              <th scope="col" class="text-center">Acciones</th>
            </tr>
      
          </thead>
          <tbody>
            <!-- Mostrar los resultados -->
            <%rows.forEach(usuario =>{%>
              
              <tr>
                <!-- se crea un registro dentro de nuestra tabla por cada registo existente dentro de la BD -->
                <td scope="row"><%= usuario.Correo%></td> <!-- Imprimimos los datos provenientes de la BD dentro de su respectiva columna -->
                <td><%= usuario.Contraseña%></td>
                <td><%= usuario.Admin%></td>
                <td class="d-flex justify-content-evenly">
                  <form METHOD="POST" clas="w-100" action="/eliminar">
                    <input type="hidden" name="id" value="<%= usuario.id%>">
                    <input type="hidden" name="tipo" value="usuario"> <!-- Para identificar que se elimina un alumno-->
                    <input type="submit" class="btn btn-danger" value="Eliminar">
                  </form>
                  <!-- añadimos un query string al final del URL el cual obtiene la id de la propiedad para detectar cual sera la propiedad a actualizar al pulsar el boton de actualizar -->
               <a href="RegistrarUsuario?id=<%= usuario.id%>" class="btn btn-warning">Actualizar</a> 
                </td>
              </tr>
              <%})%>
          </tbody>
        </table>
      
        </div>
  </body>
</html>