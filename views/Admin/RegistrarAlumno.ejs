<!DOCTYPE html>
<html lang="es">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css"/>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
    <title>Anuario: Registrar Alumno</title>
  </head>
  <body>
    <div style="padding: 0px 75px 0px 80px;" class="container-sm"> 
      <%- include('Header.ejs') %>

      <h2 style="text-align: center; margin-top: 10px;"><%= alumno.numControl ? 'Actualizar Alumno' : 'Registrar Alumno' %></h2>

      <a style="margin-bottom: 10px;" class="btn btn-primary" href="/Alumnos">Volver</a> <!--Boton volver -->
      
      <div class="form">

        <form method="post" enctype="multipart/form-data">

          <input type="hidden" name="id" value="<%= alumno.numControl || '' %>">

          <div class="form-floating mb-3">
            <input
            type="number"
            class="form-control"
            id="numControl"
            name="numControl"
            placeholder="Numero de Control"
            autocomplete="off"
            value="<%= alumno.numControl || '' %>"
            required
            >
            <label for="numControl">Numero de Control</label>
          </div>

          <div class="form-floating mb-3">
            <input
            type="text"
            class="form-control"
            id="nombreCompleto"
            name="nombreCompleto"
            placeholder="Nombre Completo"
            autocomplete="off"
            value="<%= alumno.nombreCompleto || '' %>"
            required
          />
            <label for="nombreCompleto">Nombre Completo</label>
          </div>
          
          <div class="form-floating mb-3">
            <input
              type="number"
              class="form-control"
              id="edad"
              name="Edad"
              placeholder="Edad"
              autocomplete="off"
              value="<%= alumno.Edad || '' %>"
              required
            />
            <label for="edad">Edad</label>
            
          </div>
    
          <div class="form-floating mb-3">
            <input
              type="tel"
              class="form-control"
              id="telefono"
              name="Telefono"
              placeholder="Teléfono"
              autocomplete="off"
              value="<%= alumno.Telefono || '' %>"
              required
            />
            <label for="telefono">Teléfono</label>
            
          </div>
    
          <div class="form-floating mb-3">
            <input
              type="email"
              class="form-control"
              id="correo"
              name="Correo"
              placeholder="Correo Electrónico"
              autocomplete="off"
              value="<%= alumno.Correo || '' %>"
              required
            />
            <label for="correo">Correo Electrónico</label>
            
          </div>
    
          <div class="form-floating mb-3">
            <input
              type="text"
              class="form-control"
              id="carrera"
              name="Carrera"
              placeholder="Carrera"
              autocomplete="off"
              value="<%= alumno.Carrera || '' %>"
              required
            />
            <label for="carrera">Carrera</label>
            
          </div>
    
          <div class="form-floating mb-3">
            <input
              type="number"
              class="form-control"
              id="promedio"
              name="Promedio"
              placeholder="Promedio"
              autocomplete="off"
              value="<%= alumno.Promedio || '' %>"
              min="0" max="100" step="0.01"
              required
            />
            <label for="promedio">Promedio</label>
            
          </div>
    
          <div class="form-floating mb-3">
            
            <input type="file" class="form-control" id="imagen" name="file" required> 
            <label for="imagen">Imagen</label>
            <% if (alumno.Imagen) { %>
               <div style="width: 100px; height: auto;">
                <img src="/img/<%=alumno.Imagen%>" class="img-thumbnail"> <!-- Esta clase hace mas pequeña la imagen. -->
                </div>
                <% } %>
          </div>
    
          <div class="form-floating mb-3">
            <input
            class="form-control"
            type="textarea"
             id="descripcion"
             name="habilidadesFortalezas"
             placeholder="Describe tus Habilidades y Fortalezas"
             value="<%= alumno.habilidadesFortalezas || '' %>"
             required
             >
            <label for="descripcion">Habilidades y Fortalezas</label>
        
          </div>
    
          <input type="submit" class="btn btn-success btn-lg" value="<%= alumno.numControl ? 'Actualizar' : 'Registrar' %>">
          
        </form>
    
      </div> <!--Fin Formulario-->
      
    </div> <!--Fin contenedor-->
  </body>
</html>
